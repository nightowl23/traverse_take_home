# Task Title

The infrastructure project is at `/infra/`. It contains Docker Compose, nginx, Dockerfile, and CI/CD configurations for a multi-service web application.

## What to do

<!-- Describe the infrastructure task -->

## 1. Docker Compose changes (`/infra/docker-compose.yml`)

<!-- Services, ports, volumes, environment variables -->

## 2. Nginx changes (`/infra/nginx/`)

<!-- Upstream, location blocks, headers, features -->

## 3. CI/CD changes (`/infra/.github/workflows/`)

<!-- Jobs, steps, triggers -->

## Constraints

- Don't delete existing services or configurations unless instructed
- Follow best practices for the tools involved
